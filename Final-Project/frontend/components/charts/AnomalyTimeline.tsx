export function AnomalyTimeline({ items = [] }: { items?: Array<{ label: string; score: number }> }) { return <div className="anomaly-timeline">{items.length ? items.map((item)=><div className="signal-item" key={item.label}><strong>{item.label}</strong><span>{item.score.toFixed(2)} score</span></div>) : <p className="empty-copy">No anomaly events have been ingested yet.</p>}</div>; }
